# D-RECIPE Requirements
# =====================
# MPS-compatible (Apple Silicon M-series) + CUDA fallback
# Key change: bitsandbytes is CUDA-only; on MPS use BF16 precision instead.
#
# Install:
#   pip install -r requirements_dynamic.txt
#
# For Apple Silicon (MPS):
#   pip install torch>=2.1.0 --extra-index-url https://download.pytorch.org/whl/cpu
#   (PyTorch ships with MPS support out of the box from 2.1+)

# ── Core deep learning ─────────────────────────────────────────────────────
torch>=2.1.0              # MPS support available from 2.1
transformers>=4.40.0
peft>=0.8.0               # LoRA adapters
accelerate>=1.0.0
sentence-transformers>=3.0.0

# ── Data ──────────────────────────────────────────────────────────────────
numpy>=1.26.0
datasets>=2.20.0
pandas>=2.0.0

# ── Visualisation ─────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0            # heatmaps, distribution plots
plotly>=5.18.0             # optional interactive plots

# ── Utilities ─────────────────────────────────────────────────────────────
psutil>=6.0.0              # CPU memory monitoring (MPS fallback)
tqdm>=4.66.0
scikit-learn>=1.3.0        # ROC AUC, metrics
scipy>=1.12.0
safetensors>=0.4.0
tokenizers>=0.19.0
sentencepiece>=0.2.0

# ── Logging ───────────────────────────────────────────────────────────────
wandb>=0.17.0              # optional: set --REPORT_TO wandb

# ── Dev / testing ─────────────────────────────────────────────────────────
pytest>=8.0.0

# ── REMOVED vs original requirements.txt ─────────────────────────────────
# bitsandbytes       — CUDA-only; D-RECIPE uses BF16 precision instead
# nvidia-*           — CUDA-only; not needed on MPS
# triton             — CUDA-only kernel compiler
